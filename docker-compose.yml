version: '3.8'

services:
  iptv-player:
    build: .
    container_name: iptv-player
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
    networks:
      - iptv-network

networks:
  iptv-network:
    driver: bridge
